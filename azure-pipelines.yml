trigger:
- none

pool: 
  name: Default
  demands:
    - agent.name -equals TC01

jobs:
- job: SSH_Connection
  displayName: 'Establish SSH Connection '
  steps:
  - task: CopyFilesOverSSH@0
    inputs:
      sshEndpoint: 'DENBG0811VM'
      sourceFolder: '/opt/app/siemens/test'
      contents: '**'
      targetFolder: 'C:\Users\12880\Documents'
      readyTimeout: '20000'
      failOnEmptySource: true
    
  
